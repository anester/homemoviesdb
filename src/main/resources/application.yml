micronaut:
  application:
    name: homemoviesdb
  session:
    http:
      cookie: true
      header: true
datasources:
  default:
    url: jdbc:mysql://localhost:3306/db
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: ''
netty:
  default:
    allocator:
      max-order: 3
